{
  "name": "jiitsphere-root",
  "private": true,
  "scripts": {
    "lint:all": "pnpm --filter backend lint && pnpm --filter web lint",
    "format:all": "pnpm --filter backend format && pnpm --filter web format",
    "test:all": "pnpm --filter backend test:ci && pnpm --filter web test:ci",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2"
  },
  "lint-staged": {
    "backend/src/**/*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "web/src/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
